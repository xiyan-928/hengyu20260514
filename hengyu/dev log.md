整理一下思路和步骤：



将这段代码进行优化，现在获取ddl，ph和pt100的程序是固定的，实际上对于一个固定的品牌，获取数据的流程是固定的。我需要将这个部分提取出来，根据厂商的名称自动匹配相应的读取函数。
为了更好的配置，我需要增加一个配置文件，配置每个模块的厂商型号，目前的型号为默认型号。我希望程序能够对未来更多的型号提供支持。我只需要针对特定型号提供接口即可简单使用。
另外，我希望能够灵活地增加一些配置信息，以json的形式。将modbus client的ip和port也加入到配置信息当中。这个配置要能够灵活的扩展。
在与hy_app的前端配合中，我需要前端也增加一个配置的窗口，并根据后端得到的配置信息生成合适的前端窗体，要求每一个参数均可进行配置，对于需要进行选择厂商型号的参数，需要能够提供可选项列表。